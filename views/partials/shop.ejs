
<div class="container" ng-app="airlyApp" ng-controller="shopControl as shop">
  <h1>Air.ly shop view</h1>

  <ul class="status">
    <li ng-repeat="(i, drone) in shop.drones">
      {{i}}. Drone - status: {{drone.status}}, gps: {{drone.gps}}
    </li>
  </ul>

</div>
